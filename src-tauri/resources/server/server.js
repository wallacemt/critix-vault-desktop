// Placeholder — substituído pelo `node scripts/prepare-build.mjs` antes do tauri build
// Este arquivo não é executado em produção; o server.js real é gerado pelo `next build`
throw new Error("Execute 'bun run build:app' para gerar o build de produção.");
